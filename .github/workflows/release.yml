name: goreleaser

on:
  push:
    tags:
      - "v**.**.**"
jobs:
  goreleaser:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Echo
        run: echo "Hello world!"
